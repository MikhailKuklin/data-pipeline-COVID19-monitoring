
from prefect.filesystems import GitHub

# alternative to creating GitHub block in the UI

gh_block = GitHub(
    name="covid_gh", repository="path to the script"
)

gh_block.save("de-zoom", overwrite=True)
